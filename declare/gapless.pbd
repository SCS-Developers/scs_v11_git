Declare listGaplessArray()
Declare setGaplessInfo()
Declare resetGaplessFirstAudPtrsIfReqd()
Declare setPlaylistGaplessInfo(pSubPtr)
Declare setSlideShowGaplessInfo(pSubPtr)
Declare setContainsGaplessInds(pCuePtr)
Declare clearSubGaplessInfo(pSubPtr)
Declare clearAllGaplessInds()
Declare setGaplessMajorChangeForAud(pAudPtr)
Declare setGaplessMajorChangeForCue(pCuePtr)
Declare checkForMajorChangesInEditor()
Declare PlayEndSyncProcGapless(pHandle.l, pChannel.l, pData, pUser)
